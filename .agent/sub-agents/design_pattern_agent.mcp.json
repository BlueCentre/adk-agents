{
  "mcpServers": {
    "drawio":{
      "command":"npx",
      "args":[
        "-y",
        "drawio-mcp-server"
      ]
    }
  },
  "globalServers": ["filesystem", "memory"],
  "excludedServers": ["github", "sonarqube", "playwright"]
}
